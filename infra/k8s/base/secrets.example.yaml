apiVersion: v1
kind: Secret
metadata:
  name: job-monitoring-secrets
  namespace: job-monitoring
type: Opaque
stringData:
  # Replace all values before applying. Do not commit real production secrets.
  OPENAI_API_KEY: "replace_me"
  CLIENT_REGISTRATION_PASSWORD: "replace_me"
  RESEND_API_KEY: ""
  EMAIL_FROM_ADDRESS: ""
  EMAIL_REPLY_TO: ""
  DEFAULT_CLIENT_EMAIL: ""
  MONGO_ROOT_USERNAME: "admin"
  MONGO_ROOT_PASSWORD: "replace_me"
  MONGODB_URI: "mongodb://admin:replace_me@mongo:27017"
